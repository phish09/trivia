name: Supabase Keepalive

on:
  schedule:
    - cron: '0 12 */3 * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping keepalive endpoint
        run: |
          curl -f "${{ secrets.SITE_URL }}/api/keepalive?token=${{ secrets.KEEPALIVE_SECRET }}"
